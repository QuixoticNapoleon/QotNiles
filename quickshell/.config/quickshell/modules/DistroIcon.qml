import QtQuick
import qs
import qs.components

// Distro icon (Arch).
BarBlock {
	color: Theme.bg2

	Text {
		text: "\u{f08c7}"
		color: Theme.fg
		font.family: Theme.fontIcons
		font.pixelSize: 14
	}
}
